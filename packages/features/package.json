{
  "name": "@open-zero/features",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "src/"
  ],
  "scripts": {
    "dev": "tsup --watch --onSuccess \"tsc --project tsconfig.build.json\"",
    "build": "tsup --onSuccess \"tsc --project tsconfig.build.json\"",
    "lint": "tsc --project tsconfig.build.json --emitDeclarationOnly false --noEmit && eslint src/",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.32.29",
    "@tanstack/react-query": "^5.34.2",
    "ky": "^1.2.4"
  },
  "devDependencies": {
    "@open-zero/eslint-config-node": "workspace:*",
    "@open-zero/typescript-config": "workspace:*",
    "@prisma/client": "^5.13.0",
    "@types/node": "^20.12.8",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  }
}
