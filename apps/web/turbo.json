{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "lint:type-check": {
      "outputs": ["node_modules/.tmp/**"]
    }
  }
}
